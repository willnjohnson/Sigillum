{
  "name": "sigillum",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "cli:keygen": "cargo run --manifest-path src-tauri/Cargo.toml -- keygen",
    "cli:export": "cargo run --manifest-path src-tauri/Cargo.toml -- export",
    "cli:sign": "cargo run --manifest-path src-tauri/Cargo.toml -- sign",
    "cli:verify": "cargo run --manifest-path src-tauri/Cargo.toml -- verify",
    "cli:help": "cargo run --manifest-path src-tauri/Cargo.toml -- --help"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.2.0",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2",
    "@tauri-apps/plugin-fs": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "~5.6.2",
    "vite": "^6.0.3"
  }
}
